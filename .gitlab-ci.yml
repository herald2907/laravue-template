image: jguyomard/laravel-php
stages:
  - test

unit_test:
  stage: test
  script:
    - composer update
    - cp .env.example .env
    - php artisan key:generate
    - php artisan migrate
    - phpunit --coverage-text --colors=never
    - php artisan test