image: registry.gitlab.com/JasonDelf/laravel-template:latest
stages:
  - test

unit_test:
  stage: test
  script:
    - cp .env.example .env
    - composer install
    - php artisan key:generate
    - php artisan migrate
    - vendor/bin/phpunit
    - php artisan test